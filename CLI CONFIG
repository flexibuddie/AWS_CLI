AWS COMMANDS
https://docs.aws.amazon.com/cli/latest/reference/iam/index.html#available-commands

Install AWS CLI
Type: aws --version (to confirm version installed)
aws configure list (to check existing aws configuration)
aws configure --profile default  (to configure the default using the aws access key ID and secret access key)
aws iam list-users (displays details of aws iam users)

EC2 AND CLI (Start, Stop, Reboot, and Terminate EC2 Instances)
aws ec2 start-instances --instance-ids INSTANCEID1 INSTANCEID2
aws ec2 stop-instances --instance-ids INSTANCEID1 INSTANCEID2
aws ec2 reboot-instances --instance-ids INSTANCEID1 INSTANCEID2
aws ec2 terminate-instances --instance-ids INSTANCEID1 INSTANCEID2